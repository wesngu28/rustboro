export interface DisplayPokemon {
    id: number,
    name: string,
    art: string,
    sprite: string,
    abilities: string[],
    stats: string[],
    types: string[],
    title: string,
    blurb: string,
    varieties: string[]
    evolution: string[]
}